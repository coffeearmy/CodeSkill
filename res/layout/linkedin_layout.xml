<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="?android:attr/listPreferredItemHeight"
    android:background="#eeeeee"
    android:padding="6dip" >

    <TextView
    android:id="@+id/row_toptext"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:singleLine="true"
    android:gravity="center_vertical"
    android:ellipsize="marquee"/>

     <TextView
         android:id="@+id/row_bottomtext"
         android:layout_width="fill_parent"
         android:layout_height="wrap_content"
         android:ellipsize="marquee"
         android:paddingTop="20dip"
         android:singleLine="false"
         android:textColor="#828282" />

     <ProgressBar
         android:id="@+id/chartskill"
         style="?android:attr/progressBarStyleHorizontal"
         android:layout_width="match_parent"
         android:layout_height="match_parent"
         android:layout_alignParentBottom="true"
         android:layout_alignParentLeft="true"
         android:layout_alignParentRight="true"
         android:layout_alignTop="@+id/row_toptext"
         android:layout_below="@id/row_bottomtext"
         android:max="@integer/maxprogress"
         android:progressDrawable="@layout/progressbarlayout" />

</RelativeLayout>